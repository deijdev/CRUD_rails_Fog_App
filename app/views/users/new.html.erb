<h1> Create a User </h1>

<%= render partial: "shared/error_list", locals: {new_object: @user}%>

    <%= form_for @user do |f| %>
        <%= f.label :name %>
        <%= f.text_field :name %>
        <%= f.submit "Join FOG" %>
        <% end %>